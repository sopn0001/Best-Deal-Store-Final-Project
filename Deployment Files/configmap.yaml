apiVersion: v1
kind: ConfigMap
metadata:
  name: best-deal-config
  namespace: best-deal
data:
  DB_NAME: "bestdeal"
  MONGO_URL: "mongodb://mongodb:27017"
  # Non-guest user required: RabbitMQ rejects "guest" from other pods by default.
  RABBITMQ_URL: "amqp://bestdeal:bestdeal@rabbitmq:5672"
