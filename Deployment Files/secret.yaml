apiVersion: v1
kind: Secret
metadata:
  name: best-deal-secret
  namespace: best-deal
type: Opaque
# Values are base64-encoded. Change before production use.
# echo -n 'value' | base64
data:
  MONGO_USERNAME: YWRtaW4=       # admin
  MONGO_PASSWORD: cGFzc3dvcmQ=   # password
  RABBITMQ_USER: Z3Vlc3Q=        # guest
  RABBITMQ_PASS: Z3Vlc3Q=        # guest
